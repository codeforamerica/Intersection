%h1 Cities
%br
%table.table
  - @teams.each do |team|
    %tr
      %td{:width=>'30px'}
        - if team.logo.present?
          = image_tag(team.logo.url(:icon))
      %td
        %h3
          = link_to team.name, team